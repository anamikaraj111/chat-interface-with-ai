import ChatWrapper from "@/components/ChatView/ChatWrapper";

export default function EmailInterface() {
  return (
    <div className="flex flex-col h-full font-[family-name:var(--font-geist-sans)]">
      <ChatWrapper />
    </div>
  );
}
